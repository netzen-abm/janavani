# Example environment variables

# Optional: path or HF name of a local LLM to use for generation (leave empty for retrieval-only testing)
LOCAL_LLM_MODEL=

# Directory where FAISS index and texts are persisted
RAG_INDEX_DIR=data

