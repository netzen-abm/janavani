# RETIRED: no canonical Django application is defined in the current runtime.
# Kept manually runnable temporarily so historical intent remains recoverable.
name: Django CI (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "Retired; Janavani's canonical runtime is validated by the current CI workflow."
