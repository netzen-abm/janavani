# RETIRED: no active Octopus Deploy target is configured for Janavani.
# Historical source is preserved at:
# docs/archive/github-workflows/2026-09-01/octopusdeploy.yml
name: Octopus Deploy (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "Retired; Janavani uses the canonical container deployment path."
