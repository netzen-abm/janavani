# RETIRED: no active OpenShift deployment target is configured for Janavani.
# Historical source is preserved at:
# docs/archive/github-workflows/2026-09-01/openshift.yml
name: OpenShift (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "Retired; Janavani uses the canonical container deployment path."
