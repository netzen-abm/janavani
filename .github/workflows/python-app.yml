# RETIRED: duplicate starter workflow.
# Canonical validation is provided by .github/workflows/ci.yml.
# Kept manually runnable temporarily for historical verification.
name: Python application (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Explain retirement
        run: echo "Retired; use the canonical CI workflow in .github/workflows/ci.yml."
