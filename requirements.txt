# ============================================
# Janavani Requirements — Canonical Runtime
# ============================================

# --------------------------------------------
# Web/API Framework
# --------------------------------------------
FastAPI>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.0.1
pydantic>=2.0.0
pydantic-settings>=2.0.0

# --------------------------------------------
# Existing storage/provider
# --------------------------------------------
supabase>=2.8.0

# --------------------------------------------
# Telegram channel
# --------------------------------------------
python-telegram-bot>=22.8

# --------------------------------------------
# HTTP / external adapters
# --------------------------------------------
requests>=2.32.0

# --------------------------------------------
# Directory / document processing
# --------------------------------------------
pandas>=2.2.2

# --------------------------------------------
# Feedback API
# --------------------------------------------
redis>=5.0.0

# --------------------------------------------
# PDF / editable document generation
# --------------------------------------------
weasyprint>=65.0
reportlab>=4.2.0
python-docx>=1.1.0

# --------------------------------------------
# Future capability providers
# Keep outside the canonical runtime when profiles are introduced.
# --------------------------------------------
web3>=7.0.0
ipfshttpclient>=0.7.0

# NOTE:
# Flask is intentionally not listed here. The historical src/web.py path is
# not a deployment dependency after canonical FastAPI convergence.
# NumPy is not a direct requirement. pandas installs it transitively.
