# Requirements for the SLM + RAG POC

# Core retrieval
sentence-transformers
faiss-cpu

# API
fastapi
uvicorn
python-dotenv

# Optional local generation (large models may be slow on CPU)
transformers
torch

# Dev / utils (optional)
# langchain  # not required for the minimal scaffold

# Decentralized integrations (optional)
web3
python-nostr
requests[socks]
ipfshttpclient
