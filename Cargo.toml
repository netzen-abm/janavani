[package]
name = "janavani"
version = "0.1.0"
edition = "2021"
description = "A citizen-governance bridge with modular decentralized protocol interfaces"

# This root crate currently validates protocol interface contracts only.
# Concrete third-party protocol integrations belong in dedicated adapters once implemented.

[features]
default = []

# Independent protocol interface toggles. These are intentionally dependency-free
# until the corresponding production adapters are implemented and verified.
freenet = []
nostr = []
nym = []
reticulum = []
zkp = []
blockchain = []

[dependencies]
