[package]
name = "janavani-core"
version = "0.1.0"
edition = "2021"
description = "Canonical Rust domain kernel for Janavani civic infrastructure"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
