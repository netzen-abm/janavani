"""Canonical API route facades.

During the M3-D migration, route facades preserve the existing public
implementations while giving the canonical FastAPI assembly a stable domain
module boundary. Domain implementations are migrated incrementally.
"""
